services:
  camoufox-worker:
    build: .
    ports:
      - "127.0.0.1:3002:3002"
    environment:
      - PORT=3002
      - ACCESS_KEY=${ACCESS_KEY:-}
    shm_size: '2gb'
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4g
          cpus: '2.0'
